<!-- VacationsView.xaml (обновленная версия) -->
<UserControl x:Class="TimeSheet.Views.VacationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Отпуска" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Фильтры -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                <TextBlock Text="Сотрудник" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Employees}" 
                          DisplayMemberPath="FullName"
                          SelectedItem="{Binding SelectedEmployee}"
                          Height="25">
                </ComboBox>
            </StackPanel>

            <Button Grid.Column="1" Content="Обновить" 
                    Command="{Binding LoadVacationsCommand}"
                    VerticalAlignment="Bottom" Padding="15,8" 
                    Background="#3498DB" Foreground="White"/>
        </Grid>

        <!-- Таблица отпусков -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Vacations}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Background="White" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding Employee.FullName}" Width="180"/>
                <DataGridTextColumn Header="Тип отпуска" Binding="{Binding VacationType.Name}" Width="120"/>
                <DataGridTextColumn Header="Начало" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="Окончание" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding VacationStatus.Name}" Width="100"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопка создания отпуска -->
        <Button Grid.Row="3" Content="Добавить отпуск" 
                Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"
                Command="{Binding CreateVacationCommand}"
                HorizontalAlignment="Left" Padding="20,10" 
                Background="#27AE60" Foreground="White" FontWeight="Bold"/>

        <!-- Режим операции -->
        <ContentControl Grid.Row="0" Grid.RowSpan="4"
                   Content="{Binding CurrentOperationView}"
                   Visibility="{Binding IsOperationMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>