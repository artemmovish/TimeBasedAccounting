<UserControl x:Class="TimeSheet.Views.EmployeeOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="White"
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="{Binding Title}" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Форма -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- ФИО -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="ФИО" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Employee.FullName}" Height="25"/>
                </StackPanel>

                <!-- Должность -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Должность" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Employee.Position}" Height="25"/>
                </StackPanel>

                <!-- Отдел -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Отдел" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Departments}" 
              DisplayMemberPath="Name"
              SelectedValue="{Binding Employee.DepartmentId}"
              SelectedValuePath="DepartmentId"
              Height="25"/>
                </StackPanel>

                <!-- Дата приема -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Дата приема" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding Employee.HireDate}" Height="25"/>
                </StackPanel>

                <!-- Активен -->
                <CheckBox Content="Активный сотрудник" 
                         IsChecked="{Binding Employee.IsActive}"
                         Margin="0,0,0,15"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Command="{Binding CancelCommand}"
                    Padding="15,8" Background="#95A5A6" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Command="{Binding SaveEmployeeCommand}"
                    Padding="15,8" Background="#27AE60" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>