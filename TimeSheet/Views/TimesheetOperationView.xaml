<UserControl x:Class="TimeSheet.Views.TimesheetOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="{Binding Title}" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Форма -->
        <StackPanel Grid.Row="1">
            <!-- Сотрудник -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Сотрудник" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Employees}" 
                          DisplayMemberPath="FullName"
                          SelectedValue="{Binding Timesheet.EmployeeId}"
                          SelectedValuePath="EmployeeId"
                          Height="25"/>
            </StackPanel>

            <!-- Дата -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Дата" Margin="0,0,0,5"/>
                <DatePicker SelectedDate="{Binding Timesheet.Date}" Height="25"/>
            </StackPanel>

            <!-- Отработано часов -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Отработано часов" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Timesheet.HoursWorked}" Height="25"/>
            </StackPanel>

            <!-- Маркер посещаемости -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Маркер" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding AttendanceMarkers}" 
                          DisplayMemberPath="Code"
                          SelectedValue="{Binding Timesheet.MarkerId}"
                          SelectedValuePath="MarkerId"
                          Height="25"/>
            </StackPanel>

            <!-- Комментарий -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Комментарий:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Timesheet.Comment}" Height="25"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Command="{Binding CancelCommand}"
                    Padding="15,8" Background="#95A5A6" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Command="{Binding SaveTimesheetCommand}"
                    Padding="15,8" Background="#27AE60" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>