<UserControl x:Class="TimeSheet.Views.LatenessOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="White"
             d:DesignHeight="250" d:DesignWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Добавление опоздания" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Форма -->
        <StackPanel Grid.Row="1">
            <!-- Длительность -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Длительность (минуты)" Margin="0,0,0,5"/>
                <TextBox Text="{Binding DurationMinutes}" Height="25"/>
            </StackPanel>

            <!-- Причина -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Причина" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Reason}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Command="{Binding CancelCommand}"
                    Padding="15,8" Background="#95A5A6" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Command="{Binding SaveLatenessCommand}"
                    Padding="15,8" Background="#27AE60" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>